# Fusion CAD Job Skill plan

1. Reuse the existing skill convention: strong trigger metadata, blocking questions first, deterministic assets/scripts, bounded verification, and fail-closed claims.
2. Normalize maker requirements into a versioned millimetre-based CAD Job contract.
3. Reuse installed Fusion360MCP operations; add only a minimal optional Supabase polling Add-in template.
4. Test schema validation, manufacturing rules, REST request construction, Python syntax, and skill packaging locally. Leave Fusion GUI and authenticated Supabase execution explicitly unverified.
